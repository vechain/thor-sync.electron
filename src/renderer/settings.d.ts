declare namespace Settings {
    export type NetWork = {
        name: string
        domain: string
    }

    export type Shortcut = {
        name: string
        type: string
        source: string
    }
}
