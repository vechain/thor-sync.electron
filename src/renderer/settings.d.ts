declare namespace Settings {
    export type NetWork = {
        name: string
        type: string
        rpcurl: string
    }

    export type Shortcut = {
        name: string
        domain: string
    }
}
